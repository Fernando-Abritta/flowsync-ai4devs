## 1. Backend: datos y regla de dominio

- [ ] 1.1 Crear la migración que añade `due_date` (`date`, nula) a `tasks` con `down` que la elimina; correr `node ace migration:run` y verificar que `database/schema.ts` regenerado contiene `@column.date() declare dueDate: DateTime | null` en `TaskSchema`; correr `node ace migration:rollback` y de nuevo `migration:run` para verificar que el reverso deja la tabla como estaba y que las tareas ya existentes siguen listándose sin fecha
- [ ] 1.2 Añadir al modelo `Task` el método `isOverdueOn(referenceDay: DateTime): boolean` (tiene fecha, fecha anterior al día por comparación de días, estado distinto de `done`) y crear `app/services/reference_day.ts` con `referenceDayFrom(request)` que valida `{ clientDate: request.header('x-client-date') }` con un validador VineJS de un campo `vine.date({ formats: ['YYYY-MM-DD'] }).optional()` (cabecera válida → `DateTime` a medianoche; ausente → día UTC en curso; mal formada → el `422` de VineJS con `rule: "date"` y `field: "clientDate"`, sin construir el error a mano); verificar con `npm run typecheck` y con `node ace repl` que `isOverdueOn` responde bien a los cuatro bordes (día anterior, mismo día, día posterior, sin fecha) y a `done`

## 2. Backend: validación, transformer, API

- [ ] 2.1 Añadir `dueDate: vine.date({ formats: ['YYYY-MM-DD'] }).nullable().optional()` a los validadores de creación y de actualización; verificar con `npm run typecheck`
- [ ] 2.2 Ampliar `TaskTransformer` para recibir el día de referencia en el constructor y exponer `dueDate` (`toISODate()` o `null`) e `isOverdue` (`resource.isOverdueOn(referenceDay)`); verificar con `npm run typecheck` que `TaskTransformer.transform(task, referenceDay)` tipa y que ningún otro dato de la tarea ni del responsable se expone
- [ ] 2.3 Actualizar `TasksController`: `index`, `store` y `update` resuelven el día con `referenceDayFrom(request)` y lo pasan al transformer; `store` y `update` convierten `dueDate` validada a `DateTime` (o `null`) antes de asignarla; `update` solo rechaza con `422` el cuerpo en que `status`, `assigneeId` y `dueDate` son todos `undefined`; añadir `show` con `findOrFail` y `load('assignee')`; verificar con `npm run typecheck` y `npm run lint`
- [ ] 2.4 Añadir `router.get(':id', [controllers.Tasks, 'show'])` al grupo `tasks`; arrancar el dev server para regenerar `.adonisjs/` y verificar con `node ace list:routes` que hay exactamente cuatro rutas de tareas con `auth` y que el diff generado queda para commitear
- [ ] 2.5 Verificar la API con `curl` contra el dev server, siempre con `X-Client-Date` explícito salvo donde se indique: `POST` sin `dueDate` → `201` con `dueDate: null`, `isOverdue: false`; `POST` con `dueDate` de ayer → `201` con `isOverdue: true`; `POST` con `dueDate: "2026-02-30"` → `422` `rule: "date"` `field: "dueDate"`; `PATCH` con `dueDate` de hoy → `isOverdue: false`; misma tarea con `X-Client-Date` de mañana → `isOverdue: true` sin tocarla; `PATCH` con `dueDate` de mañana → `false`; `PATCH` con `status: "done"` sobre una vencida → `false` y `dueDate` intacta; volver a `pending` → `true`; `PATCH` con `dueDate: null` → `dueDate: null`, `false`; `PATCH` con `dueDate: ""` → igual que `null`; `PATCH` con `dueDate` en formato `18/09/2026`, con hora ISO y numérica → `422` y la fecha no cambia; `PATCH` con `{}` → `422`; `PATCH` con `assigneeId` sobre una tarea con fecha → fecha e `isOverdue` intactos; `PATCH` y `POST` con `isOverdue: true` en el cuerpo → se ignora; `GET /tasks/:id` con y sin token → `200` / `401`; `GET /tasks/999999` y `GET /tasks/abc` → `404`; petición sin `X-Client-Date` → `200` con veredicto contra el día UTC; `X-Client-Date` con `hoy`, con `2026-09-18T10:00:00Z`, con `2026-9-1` y con `2026-02-30` → `422` `rule: "date"` `field: "clientDate"` también en `GET /tasks`; `GET /tasks` → cada elemento lleva `dueDate` e `isOverdue`

## 3. Frontend: tipos y cliente de API

- [ ] 3.1 Añadir a `lib/types.ts` `dueDate: string | null` e `isOverdue: boolean` en `Task` y el tipo `UpdateTaskPayload` (`status?`, `dueDate?: string | null`); en `lib/api.ts` calcular el día local del navegador (sin `toISOString`) y enviarlo como `X-Client-Date` en todas las llamadas de tareas, añadir `getTask(token, id)`, sustituir `updateTaskStatus` por `updateTask(token, id, patch)` y actualizar su único uso en la página de lista, añadir `dueDate` al mapa de etiquetas, la traducción de la regla `date` («Introduce una fecha válida.») y la de `clientDate` («No se ha podido determinar la fecha de hoy. Comprueba la fecha y la hora del equipo.»); verificar con `npm run build` y `npm run lint`, y en la pestaña de red del navegador que `GET /api/v1/tasks` sale con `X-Client-Date` igual al día local

## 4. Frontend: fila enlazada y pantalla de la tarea

- [ ] 4.1 En `TaskRow`, pintar el título como `Link` a `/tasks/:id` sin añadir ningún otro dato; verificar en pantalla que la fila sigue mostrando solo título, responsable y estado, sin fecha ni marca de vencida aunque la tarea las tenga
- [ ] 4.2 Crear la página `/tasks/:id` (`pages/task-page.tsx`): carga con indicador, aviso y enlace «Volver a la lista» si falla o no existe, título, responsable o «Sin nombre», estado en solo lectura, `Input` `type="date"` «Fecha de vencimiento» con autoguardado en `onChange` (valor completo → `updateTask` con la fecha; vacío sin `badInput` → `updateTask` con `null`; vacío con `badInput` → «La fecha no es válida o está incompleta.» sin enviar), campo deshabilitado mientras guarda, sustitución de la tarea por la respuesta, error de validación bajo el campo y el resto como aviso, señal «Vencida» con texto e icono solo cuando `isOverdue` es `true`, sin pista ni aviso cuando no hay fecha; verificar en pantalla: poner fecha de ayer → «Vencida»; hoy → sin señal; mañana → sin señal; quitar → sin señal y campo vacío sin confirmación; marcar «Hecho» desde la lista y reabrir → sin señal con la fecha intacta; fecha incompleta por teclado → mensaje y fecha anterior conservada; cambiar la fecha de una tarea de otra persona → sin advertencia
- [ ] 4.3 Añadir la ruta protegida `/tasks/:id` en `app-routes.tsx`; verificar en pantalla que sin sesión lleva a `/login`, que con sesión abre la tarea, que `/tasks/999999` muestra el aviso de tarea inexistente con el enlace de vuelta, que recargar `/tasks/:id` sigue funcionando y que «Volver a la lista» lleva a `/tasks`

## 5. Verificación final

- [ ] 5.1 Correr `npm run build`, `npm run lint` y `npm run format` en `frontend/` y `npm run typecheck`, `npm run lint` y `npm run format` en `backend/` y verificar que todos terminan sin errores ni cambios pendientes; correr `openspec validate add-task-due-date --strict`
